import { RemoveIconTitleDirective } from './remove-icon-title.directive';

describe('RemoveIconTitleDirective', () => {
  it('should create an instance', () => {
    const directive = new RemoveIconTitleDirective();
    expect(directive).toBeTruthy();
  });
});
